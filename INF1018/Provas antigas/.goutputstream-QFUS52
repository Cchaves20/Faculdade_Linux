/*
(a) (1,5 pontos)
int corta(char c1, int b)
{
return c1 + b;
}
(b) (3,5 pontos)
int buba(char *v, int n, char c)
{
int i, s = 0;
for (i=0; i<n; i++)
{
if (v[i] <= c)
s += corta(v[i], n);
}
return s;
}
*/

.globl corta
corta:
	movb %dil, %bl
	addl %dil, %ebx
	leave
	ret
