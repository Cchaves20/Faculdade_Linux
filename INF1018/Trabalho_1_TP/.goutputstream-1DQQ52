#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int gravacomp (int nstructs, void* valores, char* descritor, FILE* arquivo);

int main (void)
{
  
}

int gravacomp (int nstructs, void* valores, char* descritor, FILE* arquivo)
{
  int i, j;
  char* k[1];
  for (i = 0 ; i < strlen(descritor) + 1 ; i++, j++)
  {
    if ((descritor[i] == 'i') && (j == 0))
    {
      i1 = (int)malloc(sizeof(int));
      if (i1 == NULL)
      {
        printf("Erro de alocacao de memoria");
        exit(-1);
      }
    }
    else if ((descritor[i] == 'i') && (j == 1))
    {
      i2 = (int)malloc(sizeof(int));
      if (i2 == NULL)
      {
        printf("Erro de alocacao de memoria");
        exit(-1);
      }
    }
    else if (descritor[i] == 's')
    {
      if (j == 2)
      {
        strcat(k, descritor[i + 1]);
        strcat(k, descritor[i + 2]);
        (int)k;
        s1[k] = (char*)malloc(sizeof(char));
        if (s1 == NULL)
        {
          printf("Erro de alocacao de memoria");
          exit(-1);
        }
      }
      else if (j == 6)
      {
        strcat(k, descritor[i + 1]);
        strcat(k, descritor[i + 2]);
        (int)k;
        s2[k] = (char*)malloc(sizeof(char));
        if (s2 == NULL)
        {
          printf("Erro de alocacao de memoria");
          exit(-1);
        }
      }
      else if (descritor[i] == 'u')
      {
        u1 = (unsigned int)malloc(sizeof(unsigned int))
        if (u1 == NULL)
        {
          printf("Erro de alocacao de memoria");
          exit(-1);
        }
      }
  }
  return 0;
}
